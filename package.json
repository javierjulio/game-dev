{
  "name": "game-dev-experiments",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "prepreview": "npm run build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages --dist dist --dotfiles",
    "pyxel-examples": "uv run pyxel copy_examples"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^7.2.2"
  },
  "dependencies": {
    "bitecs": "^0.3.40",
    "ecsy": "^0.2.6"
  }
}
